// const is similar to the let but even more strict than the let
const b=100;
// if we assgin some value in const after the declaration then we got an error of syntax error
// if we assign some another value to const then we got an typerroe
// ---------

// to avoid the temporal dead zone we have to declare all varible at the top 
